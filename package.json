{
  "name": "mobile-sensor-viewer",
  "version": "1.0.0",
  "description": "Display accelerometer and gyroscope sensor values",
  "type": "module",
  "scripts": {
    "prebuild": "node ./scripts/update-build-number.js",
    "build": "tsc",
    "dev": "tsc --watch",
    "serve": "python3 -m http.server 8080",
    "gh-pages-copy": "mkdir -p docs &&cp index.html docs && cp -r dist docs && cp buildnr.txt docs/"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
